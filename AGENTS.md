# Project Instructions

本文件适用于本项目全部目录。

- 项目是 Java 后端求职作品，SQL 安全、权限隔离和可审计性优先于演示效果。
- 实现前先阅读 `README.md` 与 `docs/`；范围变化必须同步更新文档。
- 默认采用 Java 21、Spring Boot 3.x、Maven 和模块化单体，不自行拆微服务。
- 包依赖方向为 `interfaces -> application -> domain`，`infrastructure` 实现领域端口。
- 平台数据库和外部分析数据源必须使用独立配置、连接池和事务管理器。
- 任何外部数据源都按不可信边界处理；除测试样例外，只允许只读凭据。
- 不得使用正则表达式作为唯一 SQL 安全措施，不得执行未经 AST/权限/资源规则校验的 SQL。
- 不得提交真实数据库凭据，不得在日志中输出密码、完整连接串或大批业务数据。
- 新增依赖前说明用途；涉及 SQL 方言和安全规则时必须补充绕过测试。
- 未经明确范围变更，不实现写回数据库、自动修改表结构、任意 Python/JavaScript 图表代码执行和跨库联邦查询。
